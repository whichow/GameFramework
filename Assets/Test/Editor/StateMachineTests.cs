using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NUnit.Framework;

[TestFixture]
public class StateMachineTests
{
	[Test]
	public void ChangeState()
	{
		// StateMachine fsm = new StateMachine();

		// IState idleState = new IdleState();
		// IState walkState = new WalkState();
		// IState runState = new RunState();
		// IState attackState = new AttackState();
		// IState dieState = new DieState();

		// fsm.ChangeState(idleState);
		// Assert.AreEqual(fsm.CurrentState, idleState);

		// fsm.ChangeState(walkState);
		// Assert.AreEqual(fsm.CurrentState, walkState);

		// fsm.ChangeState(runState);
		// Assert.AreEqual(fsm.CurrentState, runState);

		// fsm.ChangeState(attackState);
		// Assert.AreEqual(fsm.CurrentState, attackState);

		// fsm.ChangeState(dieState);
		// Assert.AreEqual(fsm.CurrentState, dieState);
	}
}
